class Analytics {
  constructor(id, urlId, timestamp, ipAddress) {
    this.id = id;
    this.urlId = urlId;
    this.timestamp = timestamp;
    this.ipAddress = ipAddress;
  }
}

export default Analytics;